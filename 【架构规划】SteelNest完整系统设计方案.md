# 🏗️ SteelNest 跨境电商自动化系统 — 完整架构规划

> 📅 创建日期：2026-06-20  
> 🎯 目标：跨境电商独立站 + 全流程自动化链条  
> ⚠️ 本文档为长期参考架构，实际开发分阶段推进，每阶段根据运营反馈调整

---

## 一、系统全景

### 完整自动化链条

```
选品找货 → 自动询盘问价 → AI解析报价 → AI生成营销图 → 社媒自动分发 → 自动铺货上架 → 独立站销售 → 统一管理后台
   ↑            ↑             ↑            ↑              ↑              ↑             ↑             ↑
 ACCIO       Python邮件     OpenAI      AI图像API     裁剪+文案AI      同步脚本       Next.js       同一套
(预留接口)      脚本        解析回复    背景替换/美化   IG/TikTok适配   自动化流程     Vercel部署    管理面板

后台面板额外能力：💱 汇率自动同步  📊 API用量监控
```

### 模块清单（9 个）

| 编号 | 模块 | 类型 | 优先级 | 状态 |
|------|------|------|--------|------|
| ① | 独立站 Storefront | Next.js 前端 | 🔴 最高 | ✅ 已搭建 |
| ② | 自动询盘邮件 | Python 脚本 | 🟡 中期 | ⚪ 待开发 |
| ③ | AI 营销图生成 | Python + 图像API | 🟡 中期 | ⚪ 待开发 |
| ④ | 社媒素材分发 | Python + AI文案 | 🟢 后期 | ⚪ 待开发 |
| ⑤ | 自动铺货 | TypeScript 脚本 | 🟡 中期 | ⚪ 待开发 |
| ⑥ | ACCIO 对接 | 预留接口 | 🟢 后期 | ⚪ 待开发 |
| ⑦ | 汇率自动同步 | TypeScript 脚本 | 🟡 中期 | ⚪ 待开发 |
| ⑧ | API 用量监控 | 内置埋点 | 🟡 中期 | ⚪ 待开发 |
| ⑨ | 统一管理后台 | Next.js 管理面板 | 🔴 最高 | ⚪ 待开发 |

---

## 二、技术架构总览

```
steelnest/                          # 一个 GitHub 仓库管理一切
│
├── src/app/(storefront)/           # ① 独立站（已有 ✅）
├── src/app/admin/                  # ⑨ 统一管理后台（新建）
├── src/app/api/admin/              # 后台 API 路由（新建）
├── src/data/*.json                 # 所有数据存 JSON 文件（Git 版本控制）
│
├── email-worker/                   # ② 自动询盘邮件（Python，新建）
│
├── image-gen/                      # ③ AI营销图生成（Python，新建）
│   ├── generate.py
│   └── config.py
│
├── social-publisher/               # ④ 社媒素材分发（新建）
│   ├── crop-adapt.py
│   ├── generate-copy.py
│   └── platforms.json
│
├── scripts/
│   ├── sync-products.ts            # ⑤ 自动铺货
│   ├── sync-exchange-rate.ts       # ⑦ 汇率同步
│   └── track-api-usage.ts          # ⑧ API用量采集
│
├── .github/workflows/              # 定时任务 + 自动触发
│
└── docs/                           # 中文操作指南
```

### 关键设计决策

- 🚫 **不要数据库**：数据量小的阶段用 JSON 文件 + GitHub API 读写，Git 自动备份
- 🚫 **不要额外服务器**：管理后台和独立站共享同一个 Vercel 部署
- ⚠️ **写入统一走 GitHub API**：解决 Vercel 无状态文件系统和并发写入冲突（见风险分析）
- ✅ **AI 图像生成从管理后台触发**：不用 GitHub Actions 跑图像任务（省免费额度）
- ✅ **所有 API 调用带用量日志**：统一汇入用量监控面板

---

## 三、各模块详细设计

---

### 模块① 独立站 Storefront（✅ 已有）

- Next.js 16 + Tailwind CSS
- Vercel 免费部署
- Stripe + PayPal 双支付
- SEO 完整（sitemap、robots、JSON-LD、OG）
- 购物车 Context 管理
- 待扩展：管理后台入口链接、产品页汇率参考价

---

### 模块② 自动询盘邮件（Python）

```
email-worker/
├── main.py              # 主入口：发送 or 检查
├── config.py            # 邮箱配置 + 邮件模板 + 跟进策略
├── send_inquiries.py    # 批量发送（读取 suppliers.json → SMTP 发送）
├── check_replies.py     # 检查回复（IMAP 收信 → 匹配供应商 → 记录）
├── ai_parser.py         # AI 解析报价（OpenAI 提取结构化报价数据）
├── templates/           # 3 轮邮件模板（HTML）
│   ├── round_1.html     # 初始询盘
│   ├── round_2.html     # 3 天后跟进
│   └── round_3.html     # 7 天后最后提醒
├── requirements.txt
└── README.md            # 小白配置指南
```

**邮件发送方案 ⚠️（已修正）**：

| 方案 | 评价 |
|------|------|
| ~~Gmail 免费 SMTP~~ | ❌ 发英文邮件到国内企业邮箱大概率进垃圾箱 |
| **Resend（推荐）** | ✅ 你已注册，免费 100 封/天，送达率高，项目规划里本来就有 |
| 企业邮箱（腾讯/阿里） | ✅ 国内供应商送达率高，但需额外配置 |

**推荐**：前期手动发询盘走通流程 → 确认有供应商回复后 → 用 Resend API 自动化。

**反垃圾邮件策略**：
- 每封邮件间隔 30-120 秒随机延迟
- 每日限制在 Resend 免费额度内（100 封/天）
- 邮件正文纯文本 + 简单 HTML，不含追踪像素
- 自动跳过退信/无效邮箱
- 失败自动重试 3 次，间隔递增

**多轮跟进策略**：
```
第 1 轮：发送初始询盘
   ↓ 3 天后无回复
第 2 轮：温和提醒
   ↓ 7 天后仍无回复
第 3 轮：最后询问（标记为"无回复"）
```

**AI 解析报价**：收到供应商回复 → 调用 OpenAI API → 提取结构化数据：
- 产品名称、规格、单价、币种、MOQ、交期、运费、材质

---

### 模块③ AI 营销图生成

**痛点**：供应商产品图是白底棚拍图或随手拍，不适合直接放在品牌独立站。

**方案**：从管理后台手动触发（非 GitHub Actions），单张生成，避免烧免费额度。

```
供应商原图 → AI API → 生成多种风格营销图
              │
              ├── 🛒 干净白底电商图（去背景 + 统一光影）
              ├── 🌿 生活场景图（产品放在家居/户外场景中）
              ├── 🎨 品牌风格图（叠加品牌色/Logo）
              └── 📐 多尺寸适配（缩略图/详情大图/社媒图）
```

**执行方式 ⚠️（已修正）**：
- ~~GitHub Actions 批量跑~~ → 会烧完免费额度
- ✅ **管理后台按需触发**：后台点"生成营销图"→ Vercel API Route 调图像 API → 返回结果存入 `public/products/`

**技术选型**：

| API | 强项 | 费用 |
|-----|------|------|
| **remove.bg** | 一键去背景 | 免费 50 张/月 |
| **Replicate** | 开源模型（Stable Diffusion 等） | ~$0.01-0.05/张 |
| **Google Imagen** | 高质量场景生成 | ~$0.02-0.04/张 |

**每张图成本 < $0.05，按需使用不浪费。**

---

### 模块④ 社媒素材自动分发

**方案**：管理后台触发，一次生成多平台素材 + AI 文案。

```
AI生成的营销图
      │
      ▼
social-publisher/
      │
      ├── crop-adapt.py   # 自动裁剪适配
      │   ├── Instagram Feed  → 1080×1080 (1:1)
      │   ├── Instagram Story → 1080×1920 (9:16)
      │   ├── TikTok/Reels    → 1080×1920 (9:16)
      │   ├── Facebook 帖子    → 1200×630 (1.91:1)
      │   └── Pinterest       → 1000×1500 (2:3)
      │
      └── generate-copy.py  # AI 生成配套营销文案
          ├── 英文短文案 + 话题标签
          ├── 促销文案（限时折扣/包邮提醒）
          └── 品牌故事文案
```

**AI 文案生成**：调用 OpenAI API，输入产品信息 → 输出各平台适配文案。

---

### 模块⑤ 自动铺货

```
供应商产品图 → AI营销图 → 社媒适配 → 管理员审核 → 定价 → 自动上架
```

- 管理员在后台勾选，设定零售价（默认成本×3，参考实时汇率）
- 生成产品数据 → 直接通过 GitHub API 提交到仓库 → Vercel 自动部署

---

### 模块⑥ ACCIO 预留接口

- 管理后台 `/admin/accio` 页面
- 显示"等待 ACCIO 开放 API"
- 预留完整 TypeScript 接口和 API 路由框架
- API 到手后只改一个文件即可接通

---

### 模块⑦ 汇率自动同步

- 每天自动抓取 USD↔CNY 等主要货币汇率
- 免费 API：Frankfurter（完全免费，无需注册）
- 管理后台定价时显示人民币参考
- 仪表盘收入自动换算展示

---

### 模块⑧ API 用量监控

- 所有 API 调用自动记录 Token 数/张数/费用
- 仪表盘展示用量图表 + 预估月费
- 超预算 80% → 黄色预警；100% → 红色警告 + 自动暂停非必要调用

---

### 模块⑨ 统一管理后台（核心中枢）

| 页面 | 路由 | 功能 |
|------|------|------|
| 仪表盘 | `/admin` | 订单统计、询盘概览、汇率卡片、API用量 |
| 订单管理 | `/admin/orders` | 查看订单、标记发货 |
| 产品管理 | `/admin/products` | 编辑产品、上下架 |
| 询盘管理 | `/admin/inquiries` | 询盘记录、AI解析结果 |
| 供应商管理 | `/admin/suppliers` | 供应商名片 CRUD |
| 铺货中心 | `/admin/sourcing` | 选品→生成营销图→社媒适配→定价→上架 |
| 社媒素材 | `/admin/social` | 查看各平台素材+文案 |
| 汇率看板 | `/admin/exchange-rates` | 历史汇率、定价计算器 |
| API用量 | `/admin/api-usage` | 消耗明细、费用图表、预算预警 |
| 系统设置 | `/admin/settings` | 品牌/运费/邮件/API密钥/预算上限 |
| ACCIO | `/admin/accio` | 预留页面 |

认证方式：密码保护（`.env.local` 设 `ADMIN_PASSWORD`）

---

## 四、数据结构设计

所有数据存为 `src/data/*.json`（Git 版本控制）：

| 文件 | 内容 | 写入方式 | 读取方式 |
|------|------|----------|----------|
| `products.ts` | 产品数据（已有） | 铺货脚本通过 GitHub API | 独立站构建时静态读取 |
| `suppliers.json` | 供应商名录 | 管理后台 → GitHub API | 邮件脚本 |
| `inquiries.json` | 询盘记录 | 邮件脚本 → GitHub API | 管理后台 |
| `quotes.json` | AI解析报价 | 邮件脚本 → GitHub API | 管理后台+铺货 |
| `sourcing-queue.json` | 铺货队列 | 管理后台 → GitHub API | 图像脚本+铺货脚本 |
| `orders.json` | 订单记录 | Stripe Webhook → GitHub API | 管理后台 |
| `exchange-rates.json` | 汇率历史 | 汇率脚本 → GitHub API | 管理后台+独立站 |
| `api-usage.json` | API用量 | 各脚本 → GitHub API | 管理后台 |
| `settings.json` | 全局配置 | 管理后台 → GitHub API | 所有模块 |

**关键变更**：所有写入统一走 GitHub Contents API，每次修改 = 一个 Git commit，解决并发写入和数据覆盖问题。

---

## 五、⚡ 风险分析与应对方案

### 风险 1：JSON 文件并发写入（最大风险 ⛔）

**问题**：
- Vercel 是无状态 Serverless → 每个请求可能落在不同容器 → 文件修改不共享
- GitHub Actions 和 Vercel 后台同时写同一个 JSON → 数据互相覆盖
- JSON 没有事务 → 并发提交时后者覆盖前者

**解决方案**：
```
┌──────────────────────────────────────────────────┐
│              统一写入通道：GitHub API               │
│                                                    │
│  管理后台 ──PUT──> GitHub Contents API ──> commit   │
│  Python脚本 ─PUT──> GitHub Contents API ──> commit  │
│  Stripe Webhook ─PUT──> GitHub Contents API ──> commit│
│                                                    │
│  GitHub 作为唯一数据源（Single Source of Truth）      │
│  每次写入自动产生 Git commit → 天然审计追踪            │
│  GitHub 内部处理并发 → 不会出现覆盖冲突                │
└──────────────────────────────────────────────────┘
```

**实现方式**：
- 所有数据文件的读写走 `@octokit/rest`（GitHub REST API）
- 每次写入 = `GET file → 修改 → PUT file with SHA`（GitHub 的乐观锁机制）
- 如果 SHA 不匹配（说明别人已经改了）→ 自动重试
- 适合单用户管理后台场景（你是唯一管理员）

**未来升级路径**：如果后续数据量大或需要复杂查询 → 迁移到 Supabase 免费版（PostgreSQL，500MB 免费）。

---

### 风险 2：GitHub Actions 不适合跑 AI 图像生成 ⚠️

**问题**：
- Actions 免费额度 2000 分钟/月 → 每天生成 20 张图（每张 1-2 分钟）→ 一个月烧完
- AI 图像 API 调用 + 下载 + 处理 → 占用大量 Actions 时间

**解决方案**：
```
❌ 旧方案：GitHub Actions 定时批量跑图像生成
✅ 新方案：管理后台按需手动触发
   ├── 后台点"生成营销图"
   ├── Vercel API Route 直接调图像 API（几秒钟）
   ├── 结果直接写入 public/ 目录
   └── 不消耗 GitHub Actions 分钟数
```

Vercel Serverless Function 执行时间限制 10 秒（Hobby 版），单个产品图像生成一般在 3-8 秒内完成，刚好够用。一批多张图的话可以前端并发调用。

---

### 风险 3：Gmail SMTP 大量发邮件容易进垃圾箱/被封 ⚠️

**问题**：
- Gmail 发英文邮件到 QQ/163/阿里企业邮 → 大概率进垃圾箱
- 批量发送行为触发 spam 检测
- 个人 Gmail 不适合商用批量发信

**解决方案**：

| 阶段 | 方案 | 说明 |
|------|------|------|
| **前期**（现在） | 手动发询盘 | 先跑通流程，确认供应商有回复 |
| **中期**（有稳定供应商后） | Resend API | 你已注册，免费 100 封/天，送达率远高于个人邮箱 |
| **后期**（大批量） | 专用发信服务 | SendGrid / Mailgun 免费版 |

**建议**：自动化邮件等确认供应商渠道有效后再投入精力写。前期手动发 10-20 封询盘验证流程即可。

---

### 风险 4：系统长期维护成本被低估 ⚠️

**问题**：
- 每个模块都需要持续调试和适配 API 变化
- OpenAI 改模型、第三方服务涨价或关停
- JSON 文件多了之后查询和搜索需要手写
- 脚本挂了没有告警机制

**应对策略**：
1. **分阶段推进**（见第六节）—— 不要一次写全部模块，先上线能卖的核心功能
2. **API 用量监控（模块⑧）** —— 所有调用可视化，异常自动告警
3. **每个模块独立运行** —— 某个脚本挂了不影响独立站正常卖货
4. **Git 版本控制** —— 任何改动可回滚
5. **模块间松耦合** —— 通过 JSON 文件交互，不直接依赖

---

## 六、分阶段实施策略（务实版）

核心理念：**先跑通能赚钱的，再逐步自动化。** 不要一开始就写全部模块。

### 🥇 第一阶段：上线卖货（优先级最高）

```
目标：独立站上线 → 绑定域名 → 连通支付 → 能接单
工期：1-2 周
```

| 步骤 | 内容 | 状态 |
|------|------|------|
| 1.1 | 完善独立站（产品页、结账流程） | ✅ 基本完成 |
| 1.2 | 购买域名 + 绑定 Vercel + SSL | ⚪ 待做 |
| 1.3 | Stripe 测试模式 → 正式模式 | ⚪ 待做 |
| 1.4 | PayPal 接入验证 | ⚪ 待做 |
| 1.5 | 添加 Google Analytics | ⚪ 待做 |
| 1.6 | 管理后台骨架（订单查看 + 产品管理） | ⚪ 待做 |

**完工标志**：有人能通过你的域名下单并用信用卡付款。

---

### 🥈 第二阶段：管理后台实用化

```
目标：能管理订单、编辑产品、查看基础数据
工期：1-2 周
```

| 步骤 | 内容 |
|------|------|
| 2.1 | 管理后台仪表盘（订单数、收入） |
| 2.2 | 订单列表 + 标记发货 |
| 2.3 | 产品在线编辑 + 上下架 |
| 2.4 | 汇率同步（定价参考） |

---

### 🥉 第三阶段：自动化模块（按需启用）

```
前置条件：已有稳定订单 + 供应商渠道
目标：逐步接入自动化模块
```

| 步骤 | 内容 | 前置条件 |
|------|------|----------|
| 3.1 | 自动询盘邮件 v1（手动触发发信） | 有 5+ 供应商邮箱 |
| 3.2 | AI 营销图生成（后台按需触发） | 有产品需要上架 |
| 3.3 | 自动铺货（审核后一键上架） | 3.1 + 3.2 跑通 |
| 3.4 | API 用量监控 | 3.1 开始调用 AI API |
| 3.5 | 社媒素材分发 | 有社媒账号在运营 |

---

### 🏅 第四阶段：高级功能

| 步骤 | 内容 | 前置条件 |
|------|------|----------|
| 4.1 | 询盘邮件全自动化（定时 + 跟进） | 3.1 确认邮件渠道有效 |
| 4.2 | ACCIO 对接 | ACCIO 开放 API |
| 4.3 | 多语言支持 | 有非英语市场订单 |

---

## 七、部署方案

| 部分 | 部署到 | 费用 |
|------|--------|------|
| 独立站 + 管理后台 | Vercel（同一项目） | ¥0 |
| 数据存储 | GitHub 仓库（通过 GitHub API 读写） | ¥0 |
| Python 邮件脚本 | GitHub Actions（仅定时发信，不跑图像） | ¥0 |
| AI 图像生成 | Vercel API Route 按需触发 | 按量 |
| AI 文案 + 报价解析 | OpenAI API | 按量 ~$2-5/月 |
| 汇率同步 | GitHub Actions 每日 1 次 | ¥0 |
| 域名 | Cloudflare / Namecheap | ¥50-100/年 |

**第一阶段成本：域名 ¥50-100/年**  
**全模块上线后：域名 + AI API ≈ ¥300-600/年**

---

## 八、当前状态总结

| 模块 | 状态 | 备注 |
|------|------|------|
| ① 独立站 | ✅ 已搭建，代码质量高 | Next.js 16，可直接上线 |
| ⑨ 管理后台 | ⚪ 待开发 | 优先级最高，下周开始 |
| ② 邮件模块 | ⚪ 待开发 | 第二阶段，先用 Resend |
| ③ AI 营销图 | ⚪ 待开发 | 第三阶段，按需触发 |
| ④ 社媒分发 | ⚪ 待开发 | 第四阶段 |
| ⑤ 自动铺货 | ⚪ 待开发 | 第三阶段 |
| ⑥ ACCIO | ⚪ 预留 | 等 API 开放 |
| ⑦ 汇率同步 | ⚪ 待开发 | 第二阶段 |
| ⑧ API 用量 | ⚪ 待开发 | 第三阶段 |

---

> 📌 **核心原则**：不急不躁，先跑通卖货闭环，再逐步自动化。每个阶段根据实际运营反馈调整下一阶段计划。
> 
> 📅 文档日期：2026-06-20  
> 🤖 架构设计 + 风险分析：Claude Code  
> 👤 最终决策：你
