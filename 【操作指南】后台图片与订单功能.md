# 📘 操作指南：后台图片管理与订单成交闭环

> 更新日期：2026-08-23
> 适用：SteelNest 独立站（steelnest 目录）
> 本文记录本次开发的四个功能，以及怎么在本地测试和使用。

---

## 一、这次做了什么（总览）

| 功能 | 一句话说明 | 在哪用 |
|------|-----------|--------|
| ① 产品图片管理 | 后台能编辑/上传产品图片了 | 后台「产品管理」 |
| ② 图片轮播切换 | 产品详情页点小图能切换大图 | 前台产品详情页 |
| ③ 订单成交闭环 | 下单→采购→发货→通知客户，一条龙 | 前台结算 + 后台订单 |
| ④ 订单后台完善 | 统计、筛选、复制地址、备注、删除 | 后台「订单管理」 |

---

## 二、产品图片管理（后台）

### 功能说明
后台产品管理的「编辑」弹窗里，新增了**产品图片**区块，支持：
- 📤 **上传本地图片**（可多选，自动转成 base64 存进数据）
- 🔗 **粘贴图片 URL 链接**（回车或点「添加」）
- 🖼️ **缩略图预览**，悬停可「设为主图」或「删除」
- 第一张图自动作为**主图**

### 怎么用
1. 打开后台 http://localhost:3000/admin/products（密码 `admin123`）
2. 点任意产品的「编辑」
3. 在「产品名」下方找到「产品图片」区块
4. 上传图片 / 粘贴 URL → 点「保存」

---

## 三、图片轮播切换（前台）

产品详情页的图片现在可以点击切换：
- 点下方**缩略图** → 上方**大图**切换
- 当前选中的缩略图有**铜色边框**高亮

测试：打开任意产品页，如 http://localhost:3000/products/bathroom-wall-mount-steel-shower-caddy

---

## 四、订单成交闭环（核心功能）

### 4.1 完整流程（一单怎么走）

```
① 顾客在网站下单付款（信用卡/ PayPal）
        ↓
② 你收到订单（后台「订单管理」）
        ↓
③ 复制客户收货地址 → 去 1688 下单（地址填客户的）
        ↓
④ 记录供应商 + 采购成本 + 备注
        ↓
⑤ 拿到国际物流单号 → 回填 → 标记发货
        ↓
⑥ 系统自动给客户发「已发货 + 单号」邮件
```

### 4.2 顾客怎么下单（前台）

1. 打开 http://localhost:3000/shop → 加购
2. 点购物车 → 结算（Checkout）
3. 填**邮箱 + 收货地址**（新加的地址表单）
4. 选支付方式：
   - 💳 **信用卡**（本地是模拟支付，点了直接成功，用于测试）
   - 🅿️ **PayPal**（需配置 PayPal key 后才显示按钮）
5. 支付成功 → 订单自动写入 `src/data/orders.json`

### 4.3 你怎么处理订单（后台）

1. 打开 http://localhost:3000/admin/orders（密码 `admin123`）
2. 点订单的「**处理**」按钮 → 弹出详情
3. 按顺序操作：
   - 📋 **复制地址** → 粘到 1688 下单
   - 填**供应商 + 成本 + 备注** → 「保存采购」
   - 到货后 → 「标记采购完成」
   - 回填**承运商 + 物流单号** → 「标记发货」（触发邮件）
   - 可选 → 「标记完成」

---

## 五、订单后台的实用功能

| 功能 | 说明 |
|------|------|
| 📊 统计卡片 | 总订单 / 总销售额 / 待处理 / 已发货 |
| 🔍 状态筛选 | 全部 / 待处理 / 采购中 / 已采购 / 已发货 / 已完成 |
| 📋 复制地址 | 一键复制客户收货地址，直接粘到 1688 |
| 📝 采购备注 | 记录 1688 订单号、采购单号 |
| 🗑 删除订单 | 清理测试订单（带确认） |

---

## 六、本地测试步骤

### 启动项目
```bash
cd steelnest
npm run dev
```
然后打开 http://localhost:3000

### 测试图片
1. 后台产品管理 → 编辑 → 上传图片 → 保存
2. 前台产品详情页 → 点缩略图看切换

### 测试下单（完整流程）
1. /shop 加购 → 结算
2. 填邮箱 + 地址
3. 点「Credit / Debit Card」（模拟支付）
4. 成功页出现 → 订单已写入后台

### 测试订单处理
1. /admin/orders（密码 admin123）
2. 点「处理」→ 复制地址 → 填采购 → 回填单号 → 标记发货
3. 看终端：会打印一封英文发货邮件（因为还没配 Resend，所以只打印不真发）

---

## 七、注意事项

### 7.1 邮件（Resend）
- 当前**未配置** `RESEND_API_KEY`，标记发货时邮件只**打印到终端**，不真正发出
- 要真发邮件，需：注册 resend.com → 拿 API key → 填到 `steelnest/.env.local` 的 `RESEND_API_KEY`
- 发件人邮箱在 `src/data/settings.json` 的 `email.senderEmail`（正式发需在 Resend 验证域名）

### 7.2 上线 Vercel 后数据持久化
- **本地开发**（npm run dev）：订单/产品数据写入 JSON 文件，**真实保存、立即生效**
- **Vercel 线上**：文件系统只读，JSON 写入**不会持久保存**（重启/重新部署后还原）
- 要线上持久化，需接外部存储（Vercel Blob / Postgres），这是下一步的事

### 7.3 本地启动的端口冲突
如果启动报 `Port 3000 is in use`，说明有残留的 dev 进程，解决：
```bash
netstat -ano | grep :3000        # 找到 PID
taskkill /F /PID <那个PID>       # 杀掉它
npm run dev                       # 重新启动
```

### 7.4 图片体积
上传的本地图片会转成 base64 存进 JSON，**图片太大会让文件膨胀**。测试时建议用小图；正式图用外链 URL 或图床更合适。

---

## 八、改动的文件清单

| 文件 | 改动 |
|------|------|
| `src/data/products.json` | 新增：产品数据源（从 products.ts 迁出） |
| `src/data/products.ts` | 改为从 JSON 读取 + 写回 |
| `src/app/api/admin/products/route.ts` | 产品接口持久化 |
| `src/app/admin/products/page.tsx` | 产品编辑弹窗加图片管理 |
| `src/components/ProductGallery.tsx` | 新增：可切换图片画廊 |
| `src/app/products/[slug]/page.tsx` | 用画廊组件 + 动态渲染 |
| `src/components/ProductCard.tsx` / `CartDrawer.tsx` / `CartPageClient.tsx` | 图片改 `<img>`（兼容 base64/外链） |
| `src/lib/orders.ts` | 新增：订单读写/创建/更新/删除 |
| `src/lib/email.ts` | 新增：Resend 邮件（降级打印） |
| `src/app/api/orders/route.ts` | 新增：下单接口 |
| `src/app/api/admin/orders/route.ts` | 加 PUT 更新 + DELETE 删除 |
| `src/components/CheckoutForm.tsx` | 加收货地址表单 + 创建订单 |
| `src/app/admin/orders/page.tsx` | 完整订单处理 UI |
| `src/app/page.tsx` / `shop/page.tsx` | 动态渲染（编辑后前台立即生效） |

---

## 九、待办 / 下一步

- [ ] 配置 Resend（让发货邮件真发出去）
- [ ] 上线 Vercel 后接外部存储（解决数据持久化）
- [ ] Stripe 生产环境配 webhook（可靠创建订单）
- [ ] 1688 选品 + 找美国专线货代（业务侧）
